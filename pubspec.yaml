name: replaygo
description: ReplayGO sand court replay system
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.0.0
  provider: ^6.1.2
  dio: ^5.4.0
  better_player: ^0.0.84
  video_player: ^2.8.3
  shared_preferences: ^2.2.2
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  supabase_flutter: ^2.5.6

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
